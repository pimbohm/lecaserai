{% extends 'base.html.twig' %}

{% block title %}Edit Reservering{% endblock %}

{% block body %}
    {% if is_granted('ROLE_ADMIN') %}
    <h1>Edit Reservering</h1>

    {{ include('reservering/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('reservering_index') }}">back to list</a>

    {{ include('reservering/_delete_form.html.twig') }}
    {% else %}
        <h1>Geen toegang</h1>
    {% endif %}
{% endblock %}
