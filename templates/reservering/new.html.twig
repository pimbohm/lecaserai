{% extends 'base.html.twig' %}

{% block title %}New Reservering{% endblock %}

{% block body %}
    {% if is_granted('ROLE_ADMIN')  %}
    <h1>Create new Reservering</h1>

    {{ include('reservering/_form.html.twig') }}

    <a href="{{ path('reservering_index') }}">back to list</a>
    {% else %}
        <h1>Geen toegang</h1>
    {% endif %}
{% endblock %}
